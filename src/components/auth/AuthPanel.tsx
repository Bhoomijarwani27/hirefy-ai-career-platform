export default function AuthPanel(){
    return (
        <section className="relative hidden lg:flex flex-col overflow-hidden bg-white px-16 py-14">
            <div 
            className="absolute inset-0"
            style={{ 
                backgroundImage: `
                linear-gradient(to right,rgb)`

             
        </section>
    );
}    
